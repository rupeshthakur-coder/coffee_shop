import 'package:flutter/material.dart';

class TodaySpecialPage extends StatelessWidget {
  const TodaySpecialPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Today's Special Page"),
    );
  }
}
